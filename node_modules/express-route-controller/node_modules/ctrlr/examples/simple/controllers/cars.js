module.exports = {
    drive: function(from, to) {
        console.log('driving from ' + from + ' all the way to ' + to);
    },
    shiftGears: function(gear) {
        console.log('shifting gears to gear ' + gear);
    }
}
